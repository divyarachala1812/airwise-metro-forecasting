# Data folders

- `raw/` stores API responses and a checksum manifest after `scripts/download_data.py` runs. Raw JSON is ignored by Git.
- `processed/india_metro_daily_air_quality.csv` is the modelling panel at one city-day grain.

See `docs/dataset.md` for source, licence, coverage, and limitations.
